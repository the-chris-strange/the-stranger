{
  "executors": {
    "update-manifest": {
      "description": "Update package.json files in destination to match those in source",
      "implementation": "src/executors/update-manifest/executor.ts",
      "schema": "src/executors/update-manifest/schema.json"
    }
  }
}
