{
  "executors": {
    "sync-manifest": {
      "description": "Sync workspace package.json files with the root package.json",
      "implementation": "src/executors/sync-manifest/executor.ts",
      "schema": "src/executors/sync-manifest/schema.json"
    }
  }
}
